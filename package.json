{
  "name": "ticketing-system",
  "version": "1.0.0",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cross-env NODE_ENV=dev node --watch server.js",
    "start": "cross-env NODE_ENV=prod node server.js"
  },
  "author": "Jose Javier Moreno (@moreeenodev)",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "dotenv": "^17.2.2",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "cross-env": "^10.0.0",
    "morgan": "^1.10.1"
  }
}
